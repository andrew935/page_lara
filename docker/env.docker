APP_NAME=page_lara
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=page_lara
DB_USERNAME=page_lara
DB_PASSWORD=LaLala1212!@
DB_ROOT_PASSWORD=root

CACHE_STORE=file
SESSION_DRIVER=file
QUEUE_CONNECTION=database

# Cloudflare Workers Integration
# Generate a random secret: openssl rand -hex 32
# Must match the WEBHOOK_SECRET in Cloudflare Worker
CLOUDFLARE_WEBHOOK_SECRET=

